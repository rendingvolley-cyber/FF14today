# V2 validation

`test-v2-contract.mjs` checks the new homepage markers, rejects legacy homepage controls, verifies the v2 Worker API flags, and confirms `wrangler.jsonc` points to `src/v2-entry.js`.
